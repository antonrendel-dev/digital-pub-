#!/bin/bash
# SEO-аудит прямо сейчас, не дожидаясь 15-го или 30-го числа.
# Соберёт снапшот, отправит отчёт и сведёт находки с доской.
exec env FORCE_RUN=1 "$(dirname "${BASH_SOURCE[0]}")/run-audit.sh"
