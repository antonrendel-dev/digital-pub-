#!/bin/bash
# Разбор доски прямо сейчас, не дожидаясь 09:30 МСК.
#
# Обычный прогон запускается по расписанию и молча выходит, если сейчас не его
# час. Здесь проверка часа обходится через FORCE_RUN — всё остальное работает
# так же, включая замок «одна задача за раз»: если вчерашняя не закрыта,
# придёт «продолжаю вчерашнее», а не новая задача.
exec env FORCE_RUN=1 "$(dirname "${BASH_SOURCE[0]}")/run-daily.sh"
