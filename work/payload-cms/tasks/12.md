---
status: done
depends_on: [10, 11]
wave: 7
skills: [code-reviewing]
verify: []
reviewers: []
teammate_name:
---

# Task 12: Code Audit

## Required Skills

Перед выполнением задачи загрузи:

- `/skill:code-reviewing` — [skills/code-reviewing/SKILL.md](~/.claude/skills/code-reviewing/SKILL.md)

## Description

Holistic code quality review всех файлов, созданных и изменённых в этой фиче. Аудитор читает код, проверяет конкретные проблемные зоны, и пишет отчёт в `decisions.md`. Если найдены P0/P1 проблемы — спавнит агента-фиксера.

Это **audit задача**: продукт — отчёт, не код. Reviewers не назначаются — аудитор сам и есть review.

## What to do

1. **Прочитать tech-spec и user-spec:**
   - `work/payload-cms/tech-spec.md` — секции Architecture, Decisions, Data Models, Testing Strategy
   - `work/payload-cms/user-spec.md` — Acceptance Criteria и требования к безопасности

2. **Прочитать project knowledge:**
   - `.claude/skills/project-knowledge/references/project.md`
   - `.claude/skills/project-knowledge/references/architecture.md`
   - `.claude/skills/project-knowledge/references/patterns.md`

3. **Прочитать все feature-файлы** (см. раздел Context Files) и проверить каждый по чеклисту ниже.

4. **Запустить grep-проверки** (обязательно):

   ```bash
   # Нет импортов Prisma в app/ и lib/
   grep -r "from '@prisma" /home/claude/projects/digital-pub-/app/ /home/claude/projects/digital-pub-/lib/
   grep -r "from 'prisma'" /home/claude/projects/digital-pub-/app/ /home/claude/projects/digital-pub-/lib/
   grep -r "import prisma" /home/claude/projects/digital-pub-/app/ /home/claude/projects/digital-pub-/lib/

   # getPayload() не вызывается в client components
   grep -rn "getPayload" /home/claude/projects/digital-pub-/app/ /home/claude/projects/digital-pub-/lib/ /home/claude/projects/digital-pub-/components/

   # 'use client' + getPayload в одном файле — красный флаг
   grep -rln "use client" /home/claude/projects/digital-pub-/app/ /home/claude/projects/digital-pub-/components/ | xargs grep -l "getPayload" 2>/dev/null

   # revalidatePath импортируется из 'next/cache', не из других мест
   grep -rn "revalidatePath" /home/claude/projects/digital-pub-/payload/
   ```

5. **Проверить каждую зону** из чеклиста ниже.

6. **Написать отчёт в `decisions.md`** в формате:

   ```markdown
   ## Task 12 Code Audit — <дата>

   ### Summary

   <2–4 предложения: общее впечатление, количество проверенных файлов, есть ли P0/P1 проблемы>

   ### Findings

   #### P0 — Критические (блокируют деплой)

   - <finding или "нет">

   #### P1 — Важные (должны быть исправлены до деплоя)

   - <finding или "нет">

   #### P2 — Улучшения (желательно, не блокируют)

   - <finding или "нет">

   ### Grep results

   - `import prisma` в app/lib: <результат>
   - `@prisma/client` в app/lib: <результат>
   - `getPayload` в client components: <результат>
   - `revalidatePath` импорт: <результат>

   ### Files reviewed

   <список проверенных файлов>
   ```

7. **Если найдены P0 или P1 проблемы** — создать отдельную задачу-фикс (новый .md файл в `tasks/`) или описать fix прямо в отчёте с конкретными изменениями, которые нужно внести, и спавнить агента с этим файлом.

## Focus Areas (чеклист аудита)

### 1. getPayload() только в server context

- `getPayload()` вызывается только в server компонентах, API route handlers, afterChange hooks, server actions — НЕ в файлах с `'use client'`
- Каждый вызов `getPayload()` делается внутри async функции (не на module level)
- Import `{ getPayload } from 'payload'` присутствует только в серверных файлах
- `payload.config.ts` импортируется через `import config from '@payload-config'` (алиас), не по прямому пути

### 2. Consistent error handling в lib/\*

- `lib/tags.ts`: все функции имеют try/catch; в catch — `console.warn('[tags] ...')` и возвращают null/[] (не бросают)
- `lib/posts.ts`: аналогично — try/catch, `console.warn('[posts] DB unavailable ...')`, возвращают `{ posts: [], total: 0, totalPages: 0 }` или null
- Нет голых `throw` без обработки в публичных функциях lib/\*
- `app/api/articles/route.ts`: все error paths возвращают Response с правильным status (401, 400, 500) — нет unhandled promise rejections

### 3. Нет Prisma imports

- Нет `import { PrismaClient }` или `from '@prisma/client'` в `app/`, `lib/`, `components/`
- `lib/prisma.ts` удалён (или его импорты нигде не используются)
- `prisma/seed.ts` удалён (не используется)
- `scripts/migrate-tags.ts` и `scripts/migrate-posts.ts` могут импортировать Prisma (они одноразовые миграционные скрипты — это нормально)
- `scripts/sync-telegram.ts` не использует Prisma (заменён на Payload REST API)

### 4. afterChange hook correctness

- Каждый hook в `payload/collections/*.ts` импортирует `revalidatePath` из `'next/cache'`
- Tags collection: hook вызывает ``revalidatePath(`/vacancies/${doc.slug}`, 'page')`` (с реальным slug документа, НЕ с шаблоном Next.js роута) + `revalidatePath('/', 'layout')`
- Posts collection: hook итерирует `doc.tags` (relationship array) и вызывает ``revalidatePath(`/vacancies/${tag.slug}`, 'page')`` для каждого тега поста (НЕ использует `doc.slug` — slug поста ≠ slug категории). Плюс `revalidatePath('/vacancies', 'layout')`
- Articles collection: hook вызывает `revalidatePath('/articles', 'layout')` + ``revalidatePath(`/articles/${doc.slug}`, 'page')``
- Globals (Navbar, Footer, SocialChannels, SiteSettings): каждый Global вызывает `revalidatePath('/', 'layout')`
- Hook функции объявлены как `async` (или корректно возвращают Promise)
- Hook не вызывает `revalidatePath` с undefined/null значениями — slug guard проверен

### 5. FeedPost type alignment

- `lib/postUtils.ts` — тип `FeedPost` отражает поля Payload (не Prisma): `tags` — массив объектов `{ id, slug }` (не строк), `channelUsername` и `telegramMessageId` присутствуют, поля в camelCase соответствуют Payload field names
- `lib/posts.ts` маппинг: Payload возвращает `relationship` поля как объекты — убедиться что `FeedPost.tags` собирается корректно (не `undefined`)
- `getPrimaryCategorySlug` в `postUtils.ts` работает с новой структурой tags (объекты `{ slug }`, не строки)

### 6. Access control correctness

- `payload/collections/users.ts`: `create: () => false` (нет self-registration)
- `payload/collections/articles.ts`: `create` разрешён для `agent` и `admin`; `update`/`delete` — только `admin`; публичный `read` возвращает только `{ status: { equals: 'published' } }` для неавторизованных
- `payload/collections/posts.ts`: `create` разрешён для `sync` и `admin`; `update`/`delete` — только `admin`
- `payload/collections/tags.ts`: только `admin` может создавать/редактировать/удалять
- `app/api/articles/route.ts`: auth проверяется через `payload.auth({ headers })` без `overrideAccess: true`; токен из заголовка `Authorization: users API-Key <token>` корректно передаётся в Payload

### 7. sync-telegram.ts безопасность

- API Key (`PAYLOAD_API_KEY`) не логируется в error messages (только status code и URL)
- `Authorization` header не появляется в console.error/console.warn
- Деградация на 409: пост пропускается без инкремента счётчика ошибок

## Acceptance Criteria

- [ ] Отчёт записан в `decisions.md` с разбивкой по severity (P0/P1/P2)
- [ ] Grep результаты по Prisma imports задокументированы
- [ ] Grep результаты по getPayload в client components задокументированы
- [ ] Все 6 зон аудита пройдены и результаты записаны
- [ ] Если найдены P0/P1 — созданы задачи на исправление или описаны конкретные правки

## Context Files

- [tech-spec.md](../tech-spec.md) — Architecture, Decisions, Data Models
- [user-spec.md](../user-spec.md) — Acceptance Criteria, security requirements
- [decisions.md](../decisions.md) — сюда пишется отчёт аудита
- [architecture.md](../../.claude/skills/project-knowledge/references/architecture.md)
- [patterns.md](../../.claude/skills/project-knowledge/references/patterns.md)
- [project.md](../../.claude/skills/project-knowledge/references/project.md)
- [payload/collections/tags.ts](../../../payload/collections/tags.ts)
- [payload/collections/posts.ts](../../../payload/collections/posts.ts)
- [payload/collections/articles.ts](../../../payload/collections/articles.ts)
- [payload/collections/media.ts](../../../payload/collections/media.ts)
- [payload/collections/users.ts](../../../payload/collections/users.ts)
- [payload/globals/navbar.ts](../../../payload/globals/navbar.ts)
- [payload/globals/footer.ts](../../../payload/globals/footer.ts)
- [payload/globals/socialChannels.ts](../../../payload/globals/socialChannels.ts)
- [payload/globals/siteSettings.ts](../../../payload/globals/siteSettings.ts)
- [lib/tags.ts](../../../lib/tags.ts)
- [lib/posts.ts](../../../lib/posts.ts)
- [lib/postUtils.ts](../../../lib/postUtils.ts)
- [app/sitemap.ts](../../../app/sitemap.ts)
- [app/api/articles/route.ts](../../../app/api/articles/route.ts)
- [scripts/sync-telegram.ts](../../../scripts/sync-telegram.ts)
- [scripts/migrate-tags.ts](../../../scripts/migrate-tags.ts)
- [scripts/migrate-posts.ts](../../../scripts/migrate-posts.ts)

## Verification Steps

**Automated:** N/A — это audit задача, не code задача; автоматизированных тестов нет.

**Smoke:** После завершения аудита убедиться, что отчёт записан и содержит разбивку по severity:

```bash
# Проверить наличие отчёта аудита в decisions.md
grep -c "Task 12 Code Audit" work/payload-cms/decisions.md

# Проверить наличие секций P0/P1/P2 в отчёте
grep -c "P0" work/payload-cms/decisions.md
grep -c "P1" work/payload-cms/decisions.md
```

## Details

### Files

Задача читает (не изменяет) следующие файлы:

- `payload/collections/tags.ts`, `posts.ts`, `articles.ts`, `media.ts`, `users.ts` — коллекции Payload с access control и afterChange hooks
- `payload/globals/navbar.ts`, `footer.ts`, `socialChannels.ts`, `siteSettings.ts` — Globals с revalidatePath hooks
- `lib/tags.ts`, `lib/posts.ts`, `lib/postUtils.ts` — слой доступа к данным (бывший Prisma, теперь Payload)
- `app/sitemap.ts`, `app/api/articles/route.ts` — серверные endpoints
- `scripts/sync-telegram.ts`, `scripts/migrate-tags.ts`, `scripts/migrate-posts.ts` — служебные скрипты

Единственный файл, куда аудитор **пишет** — `work/payload-cms/decisions.md` (отчёт).

### Dependencies

- Зависит от Task 10 (миграция lib/ с Prisma на Payload) и Task 11 (sync-telegram переход на Payload REST API) — обе должны быть завершены, чтобы аудировать финальное состояние кода.
- Если Task 10 или 11 ещё не завершены — не запускать Task 12.

### Edge cases

- **Найдены P0 проблемы** (например, `getPayload()` в client component, или `import prisma` в app/) — аудитор создаёт новый файл `work/payload-cms/tasks/12-fix.md` с конкретными правками и спавнит агента-фиксера. Деплой блокируется до устранения.
- **Найдены только P1 проблемы** — описать конкретные правки в отчёте, создать fix-задачу, предупредить что деплой нежелателен без исправлений.
- **Файл не существует** (например, `lib/prisma.ts` уже удалён) — отметить как ✅ в отчёте, не считать ошибкой.
- **Grep возвращает 0 результатов** — это хороший знак, фиксировать как "нет нарушений".

### Implementation hints

Ключевые grep-команды для быстрой проверки критичных нарушений:

```bash
# P0-кандидаты: Prisma imports вне scripts/
grep -rn "from '@prisma\|from 'prisma'\|import prisma" app/ lib/ components/ 2>/dev/null

# P0-кандидаты: getPayload в client components
grep -rln "'use client'" app/ components/ 2>/dev/null | xargs grep -l "getPayload" 2>/dev/null

# P1-кандидаты: revalidatePath не из next/cache
grep -rn "revalidatePath" payload/ | grep -v "from 'next/cache'"

# Формат отчёта: добавить в decisions.md секцию Task 12 Code Audit с датой,
# затем заполнить Summary, Findings (P0/P1/P2), Grep results, Files reviewed.
```

## Reviewers

Это audit задача — продукт аудитора (отчёт в `decisions.md`) сам является результатом проверки. Дополнительные ревьюеры не назначаются.

## Post-completion

- [ ] Отчёт аудита записан в `decisions.md`
- [ ] Если P0/P1 найдены — фикс-агент запущен или задача создана
- [ ] Статус задачи обновлён до `done`
